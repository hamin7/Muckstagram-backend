type Mutation {
    createAccount(
        # !가 붙는것은 꼭 필요하다는 뜻, required
        username: String!
        email: String! 
        firstName: String 
        lastName: String 
        bio: String
        ): Boolean!
}
